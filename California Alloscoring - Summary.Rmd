---
title: "California Alloscoring - Summary"
author: "Emily Tyszka"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE, message=FALSE}

library(covidHubUtils)
library(targets)
library(ggbump)
library(kableExtra)
library(tidyverse)

### Score computation
tar_load("truth_data")
tar_load("forecast_data1")
tar_load("forecast_data2")
tar_load("forecast_data3")
tar_load("forecast_data4")
tar_load("alloscore1")
```

# Background

## Forecast Scoring

In recent decades, and especially following the onset of the COVID-19 pandemic, infectious disease forecasting has been of increasing interest to public health practitioners, governments, and executive-level decision-makers both within and outside of the healthcare and public health space. When forecasts produce advance estimates of disease burdens and resource use (such as healthcare visits and hospitalizations) that are accurate and actionable, leaders are able to make proactive policy and organizational decisions that lessen morbidity, prevent mortality, address needs related to disease outbreaks, and reduce avoidable resource waste. [Health departments and hospital use examples]

In the development of these forecasts, teams utilize a wide range of inputs and methodologies, and as a result models may vary widely in their forecast accuracy. Furthermore, model performance may be context-specific - models that perform well in one setting or time period may produce inaccurate predictions and decisions in another, due to differences in the qualities of the population of interest, variation in data accuracy/timeliness, differences in disease dynamics, unaccounted-for variables, or other factors. For this reason, the question of what is the "best model" for a time or context requires standardized methods of scoring and assessment. In response to this need, a number of methods of model assessment have been developed, which quantitatively "score" forecasts on their success predicting outcomes.

## Existing scoring methods

Currently, a number of scoring methods exist across the infectious disease forecasting space. All methods evaluate a given forecast against the truth (the final outcome, of which the forecasters had no knowledge at time of prediction) for a given target (count at a date and time horizon) and then aggregate those into a total score for the model as a whole over the period tested. They are detailed briefly below:

-   **Mean Absolute Error (MAE)**: Mean absolute deviation of the prediction from the true value. On the same scale as the prediction. This measure uses point estimates only.

-   **Root Mean Square Error (RMSE)**: Average deviation of the prediction from the true value, calculated by taking the square root of the average squared residuals. On the same scale as the prediction. This measure uses point estimates only.

-   **Logarithmic Score (LS):** The log of the probability assigned to the observed outcome by the forecast. The higher, the better, since it means that the forecast marked the outcome more "likely"

-   **Continuous Ranked Probability Score (CRPS):** For a prediction is given as a distribution, this score compares the CDF of the predicted values with the step-function CDF of the true value (where cumulative probability is 0 at values less than the true value, 1 at values above). The 2 curves are overlaid and the area between them is measured as the squared difference between the forecasted and observed CDFs. A lower CRPS is indicative of better model performance, as it differs less than the stepwise function that is being used as a stand-in for the truth.

-   **Weighted Interval Score (WIS)**: An interval score expresses the width of the interval, with larger intervals receiving higher scores (higher scores indicate less accuracy) and incorporates a penalty if the interval does not cover the eventual observation, which increases the further away the interval is from the observed value. The WIS weighted sum of interval scores at different probability levels, based on their PI width.

    -   **Mean WIS**

## Allocation Score

When selecting a forecast or forecasts to make predictions of a given infectious disease outcome, the above methods can serve as quantitative measures of forecast accuracy, allowing for easy comparison between model performances. All of the existing methods described above, however, evaluate the forecasts entirely on whether [how correct]{.underline} a forecast's predictions can be considered to be, which may or may not translate to [well-informed decision-making]{.underline} when the model is applied in practice.

The allocation score, introduced by [Gerding et al. in 2024](https://academic.oup.com/jrsssa/article/188/4/1299/7927906), scores forecasts instead on their success in allocating limited medical resources across multiple locations. For a given forecast date and time horizon, an forecast is evaluated across all locations together.

### Overview:

In its simplest form, this scoring framework:

1.  Identifies a set of limited resources\* of count K, which will be distributed across the locations based on their forecasts.

2.  Compiles a set of disease burden forecasts for multiple locations (ex. all counties in a state) and distributes the limited resources according to the forecast's prediction of disease burden (ie. predicted need) in those locations.

3.  Determines the true need across all locations from the true counts (finalized after the forecasted date has passed)

4.  Generates an ideal "oracle" distribution that would be made by a decision-maker with knowledge of the true need

5.  Calculates unmet need:

    1.  [Total unmet need]{.underline}: Forecasts that distribute fewer units of resource than required in a given location leave unmet need. This is summed across all locations. Where more resources than are needed are allocated, this number is 0.
    2.  [Total unavoidable unmet need]{.underline}: In cases of resource constraints, it is not possible to distribute resources such that all locations meet their needs. If the oracle distribution requires more units of resource than exist in the distribution problem, the difference is defined as unavoidable unmet need. This is summed across all locations.
    3.  [Total avoidable unmet need]{.underline} = total unmet need - total unavoidable unmet need

6.  This count of total avoidable unmet need is the [**allocation score (AS)**]{.underline}.

###### *\*Note: the resources in question remain theoretical, but are considered to be available for distribution to the locations of interest. For example, ventilators, PPE, or mobile staffing such as travel nurses may all be "sent" to a location that predicts higher resource need. Hospital beds, while in some ways able to be distributed (as through the setup of field hospitals) are less mobile and perhaps less suited to this particular thought experiment. Regardless, the actual resource does not matter, so long as it may be distributed across locations based on a set of forecasts, its need is expressed by the disease burden forecasts of interest, and it can be "wasted" if incorrectly distributed.*

### Example:

Below, we see an simplified illustration of an allocation scoring scenario. In this scenario, 20,000 units of resources are to be distributed between 4 counties of different populations at a set point in time. 3 allocation methods are being scored: 1 allocation proportional to county population and allocation according to the predictions of 2 forecasts. On the far right, the oracle distribution is written in grey and exhibits 800 units of inevitable loss.

![](Allocation%20Score%20Diagrams/1.png)

The 3 allocations are scored by first determining the loss for each location and model (ie. tallying all underallocation - where the allocation is less than the true resource need). Overallocation is not penalized.

For each allocation, this is summed, then unavoidable loss is subtracted from the value to give the alloscore. (For example, allocation by population had a total 3,000 units of loss - subtracting 800 gives the alloscore of 2,200.)

![](Allocation%20Score%20Diagrams/2.png)

With this method, we can treat the allocation by population as a control and judge our two forecasts with regards to how well they inform resource distribution decisions in the scenario of interest, compared to an allocation that ignores predicted disease burden - in this example, Forecast 1 scored better, while Forecast 2 scored worse.

### AS for Probabilistic Forecasts:

Expanding upon this method for use with probabilistic forecasts, where predictions are expressed in terms of quantiles of a probability distribution, the basic allocation score method is expanded by:

-   Generating a *loss function*, which expresses the sum of contributions from unmet need in each location, for a given allocation.

-   Incorporating the probability distributions of each forecast, find the expected value of the loss function across all locations for the allocation of interest.

-   Selecting the allocation which minimizes the value of the loss function.

### Some additional notes:

Additional considerations for the allocation score:

-   "At its core, the AS measures whether the forecast accurately captures the relative magnitudes of resource need across different locations, which is precisely the information that is needed to allocate resources to those locations subject to a fixed resource constraint." - Gerding et al.

-   The raw numerical values of the allocation score are dependent upon the resource constraint established when selecting K. All else being equal, with a lower selected K we can expect a higher AS, since there are a lower number of resources to distribute relative to observed need.

    -   As a result of this, it should be noted that raw AS values *between* experiments are not comparable. Two compare the raw AS values of two forecasts, they should be subjected to the same experiment.

    -   As an additional extension to the AS, the calculation of **Integrated Allocation Score (IAS)** addresses uncertainty with regards to resource levels. This score is generated by determining AS across a grid of discrete possible K values, weighed by their probabilities. (CHECK HOW THE CODE RAN THIS - WHAT IS MIN AND MAX K? WHAT P(K) DISTRIB USED TO WEIGH?)

# Project Justification and Goals

-   As far as we are aware, no researchers have attempted running the allocation score following the initial paper publication. A major goal of this project is to repeat the scoring methodology on a new set of epidemic data and evaluate its performance and relevance to decisions made during the period of interest

-   California was selected due to:

    -   Being a large population state with 58 counties of varying populations

    -   Robust forecast-related infrastructure and recordkeeping by the California Department of Public Health’s (CDPH) COVID Modeling Team

    -   The frequent usage of COVID-19 forecasts throughout the early pandemic by state level decision-makers.

    -   Existing relationships with the CA COVID Modeling Team, who can consult upon scoring results and their applicability to the decisions that were made during the period of interest

-   This project attempts to replicate the initial national-level + statewide analysis on all 58 counties in California during a high burden period.

    -   In [*Assessing the accuracy of California county level COVID-19 hospitalization forecasts to inform public policy decision making*](https://link.springer.com/article/10.1186/s12889-023-15649-0), White *et* *al.* evaluated forecasts during the Alpha through early Omicron waves. As a result, this project chooses the Delta and Omicron waves as time periods of interest.

    -   Models of interest will be selected for consistent reporting across all counties + statewide, and coverage of 5+ quantiles during the time period in question. Duplicate models/models from the same teams with methodological overlap will be dropped.

# Experimental Outline

1.  **Select a subset of models that submitted forecasts on the weeks of 11/21/2020 and 12/25/2021.** Additional criteria include:
    1.  Forecasts must submit predictions for all CA counties.
    2.  Predictions must include at least 5 quantiles
    3.  Forecasts must include predictions for the 1 week, 2 week, 3 week, and 4 week time horizons
        1.  11/21/2020: target dates of 2020-11-28, 2020-12-05, 2020-12-12, and 2020-12-19
        2.  12/25/2021: target dates of 2022-01-01, 2022-01-08, 2022-01-15, and 2022-01-22
    4.  As a control, a per capita allocation will be used: *allocation based on county census population*
2.  **Run the allocation score for the selected forecasts on the 2 dates:**
    1.  For each time period of interest, select a resource count. These counts should make loss inevitable, so that allocation scores are meaningful (with too many resources, AS values could all be 0)
    2.  For each model, week, and horizon, calculate the allocation score across all locations.
3.  **Compare allocation score results to mWIS (and other?) scoring methods:**
    1.  Visualize score for each model by week for the selected horizons (like plot Nick quickly made)
        1.  Put next to the same for WIS
    2.  Visualize relative ranks over time (is there a good non spaghetti way to do this?)
        1.  And is there any way to compile this? Metrics average ranking, etc.
    3.  Visualize loss (it's in the manuscript)
    4.  Select 3 top models: visualize (time series graphs in manuscript)
4.  **[Further rabbitholes]**
    1.  Regional evaluation as done in the CDPH paper
    2.  Experiment with what model missingness does

# Time Period of Interest

## Statewide

```{r echo=FALSE}

truth_data_ca <- truth_data %>% filter(`location_name` == "California")

dates_of_interest <- truth_data %>% filter(target_end_date == "2020-11-21" | 
                                             target_end_date == "2021-12-18") %>% 
  filter(location_name == "California")

horizons_of_interest <- truth_data %>% filter(target_end_date == "2020-11-28" | 
                                                target_end_date == "2020-12-05" |
                                                target_end_date == "2020-12-12" |
                                                target_end_date == "2020-12-19" |
                                             target_end_date == "2021-12-25"|
                                             target_end_date == "2022-01-01"|
                                             target_end_date == "2022-01-08"|
                                             target_end_date == "2022-01-15") %>% 
  filter(location_name == "California")

ggplot(data=truth_data_ca, aes(x=target_end_date, y=value)) + 
  geom_point(color="grey75") + geom_line(color="grey75") + labs(x = "MMWR Week End Date", y ="Weekly Incident Cases", title = "California: Statewide Weekly Incident COVID-19 Cases", subtitle = "cyan = forecast date; pink = target dates")  + theme_bw() + theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust=1)) + geom_point(data = dates_of_interest, aes(x=target_end_date, y=value), color="#00BFC4") + geom_point(data = horizons_of_interest, aes(x=target_end_date, y=value), color="#C77CFF")


```

# Data Sources and Selection

## COVID-19 Forecast Data

## Model Selection

For this section, we will select the models to run by finding those that reported for all counties + CA on a given forecast date. California has 58 counties, so we expect to have 58 predictions for a given forecast date, time horizon, and quantile.

We will therefore visualize, by forecast date + quantile, how many predictions each model submitted for each horizon.

```{r include=FALSE, echo=FALSE}

# locations - CA state + CA county
data("hub_locations")
locations <- hub_locations |>
  dplyr::filter(geo_type == "county" & (abbreviation %in% c("CA")) ) |>
  dplyr::pull(fips)
values <- c(forecast_dates = c(as.Date("2020-11-21"), as.Date("2021-12-16"), as.Date("2021-12-17"),
                               as.Date("2021-12-18"), as.Date("2021-12-19"), as.Date("2021-12-20"), 
                               as.Date("2021-12-21")))
allforecasts <- load_forecasts(
    dates = values,
    date_window_size = 6,
    #models = "COVIDhub-ensemble",
    locations = locations,
    types = c("quantile"),
    targets = paste(1:4, "wk ahead inc case"),
    source = "zoltar",
    verbose = FALSE,
    as_of = NULL,
    hub = c("US"))

## Test if pull looks good
# california <- allforecasts %>% filter(location_name == "California")
# counties <- allforecasts %>% filter(location_name != "California")
# head(counties)

## For a given model, quantile, and horizon (in weeks), what is the date-by-date count of predictions? 
##California has 58 counties, so counties + statewide would be 59 predictions - that would be an ideal number to shoot for.

pointestimatecompleteness <- as.data.frame(table(allforecasts$model, allforecasts$forecast_date, allforecasts$horizon, allforecasts$quantile))
pointestimatecompleteness <- rename(as_tibble(pointestimatecompleteness), Model = Var1, Date = Var2, "Horizon (Weeks)" = Var3, Quantile = Var4)

pointestimatecompleteness$Freq <- as.numeric(pointestimatecompleteness$Freq)
pointestimatecompleteness <- pointestimatecompleteness %>% dplyr::mutate(FreqDiscrete = case_when(
  Freq==0 ~ "No Prediction",
  Freq==1 ~ "1 Prediction",
  Freq>1 & Freq< 58 ~ "Between 1 and 57 Predictions",
  Freq==58  ~ "Exactly 58 Predictions",
  Freq> 58 & Freq < 100 ~ "59-99 Predictions",
  Freq > 99 & Freq <  200 ~ "100-200 Predictions",
  Freq > 200 ~ "Over 200 Predictions",
  TRUE ~ NA
))
pointestimatecompleteness$FreqDiscrete <- factor(pointestimatecompleteness$FreqDiscrete, 
                                                 levels = c("No Prediction", "1 Prediction",
                                                            "Between 1 and 57 Predictions", 
                                                            "Exactly 58 Predictions", 
                                                            "58-99 Predictions", 
                                                            "100-200 Predictions",
                                                            "Over 200 Predictions"))


pointestimatecompleteness$Date <- as.Date(pointestimatecompleteness$Date)
library(viridis)

```

### November 21st, 2020 {.tabset}

The 50% and 2.5% quantiles show the same number of reports in total for each For the November 21st, 2020 date, we see forecasts reported for the following:

-   UVA-Ensemble

-   JHUAPL-Bucky

-   COVIDhub-ensemble

-   COVIDhub-baseline

-   COVIDhub-4_week_ensemble

-   COVIDhub_CDC-ensemble

-   CEID-Walk

***However***, we can see here that there are multiple COVID Hub ensembles being reported. We will select the 4 week ensemble, resulting in the following 4 models to examine for this forecast date:

-   **UVA-Ensemble**

-   **JHUAPL-Bucky**

-   **COVIDhub-4_week_ensemble**

-   **CEID-Walk**

#### 50%

```{r echo=FALSE}
countovertime_Nov21_50 <- pointestimatecompleteness %>% filter(Quantile == 0.5, Date == "2020-11-16") %>% ggplot(aes(y=Model, x=`Horizon (Weeks)`, fill=FreqDiscrete))+
  geom_tile(color= "white") + 
  scale_fill_viridis(name="Count of Predictions (Counties = 58)", direction=-1, discrete=TRUE) + 
  theme(axis.text.x = element_text(angle = 45, vjust = .5, hjust = .5)) +
  ggtitle("2020-11-16: Count of Submissions by Model and Horizon", subtitle = "Quantile = 0.500")
countovertime_Nov21_50
```

#### 25%

```{r echo=FALSE}
countovertime_Nov21_25 <- pointestimatecompleteness %>% filter(Quantile == 0.25, Date == "2020-11-16") %>% ggplot(aes(y=Model, x=`Horizon (Weeks)`, fill=FreqDiscrete))+
  geom_tile(color= "white") + 
  scale_fill_viridis(name="Count of Predictions (Counties = 58)", direction=-1, discrete=TRUE) + 
  theme(axis.text.x = element_text(angle = 45, vjust = .5, hjust = .5)) +
  ggtitle("2020-11-16: Count of Submissions by Model and Horizon", subtitle = "Quantile = 0.250")
countovertime_Nov21_25
```

#### 10%

```{r echo=FALSE}
countovertime_Nov21_10 <- pointestimatecompleteness %>% filter(Quantile == 0.10, Date == "2020-11-16") %>% ggplot(aes(y=Model, x=`Horizon (Weeks)`, fill=FreqDiscrete))+
  geom_tile(color= "white") + 
  scale_fill_viridis(name="Count of Predictions (Counties = 58)", direction=-1, discrete=TRUE) + 
  theme(axis.text.x = element_text(angle = 45, vjust = .5, hjust = .5)) +
  ggtitle("2020-11-16: Count of Submissions by Model and Horizon", subtitle = "Quantile = 0.100")
countovertime_Nov21_10
```

#### 2.5%

```{r echo=FALSE}
countovertime_Nov21_2.5 <- pointestimatecompleteness %>% filter(Quantile == 0.025, Date == "2020-11-16") %>% ggplot(aes(y=Model, x=`Horizon (Weeks)`, fill=FreqDiscrete))+
  geom_tile(color= "white") + 
  scale_fill_viridis(name="Count of Predictions (Counties = 58)", direction=-1, discrete=TRUE) + 
  theme(axis.text.x = element_text(angle = 45, vjust = .5, hjust = .5)) +
  ggtitle("2020-11-16: Count of Submissions by Model and Horizon", subtitle = "Quantile = 0.025")
countovertime_Nov21_2.5
```

### December 18th, 2021 {.tabset}

#### 50%

```{r echo=FALSE}

countovertime_Dec19_50 <- pointestimatecompleteness %>% filter(Quantile == 0.5, Date == "2021-12-19") %>% ggplot(aes(y=Model, x=`Horizon (Weeks)`, fill=FreqDiscrete))+
  geom_tile(color= "white") + 
  scale_fill_viridis(name="Count of Predictions (Counties = 58)", direction=-1, discrete=TRUE) + 
  theme(axis.text.x = element_text(angle = 45, vjust = .5, hjust = .5)) +
  ggtitle("2021-12-19: Count of Submissions by Model and Horizon", subtitle = "Quantile = 0.500")
countovertime_Dec19_50
```

#### 25%

```{r echo=FALSE}
countovertime_Dec19_25 <- pointestimatecompleteness %>% filter(Quantile == 0.25, Date == "2021-12-19") %>% ggplot(aes(y=Model, x=`Horizon (Weeks)`, fill=FreqDiscrete))+
  geom_tile(color= "white") + 
  scale_fill_viridis(name="Count of Predictions (Counties = 58)", direction=-1, discrete=TRUE) + 
  theme(axis.text.x = element_text(angle = 45, vjust = .5, hjust = .5)) +
  ggtitle("2021-12-19: Count of Submissions by Model and Horizon", subtitle = "Quantile = 0.250")
countovertime_Dec19_25
```

#### 10%

```{r echo=FALSE}
countovertime_Dec19_10 <- pointestimatecompleteness %>% filter(Quantile == 0.10, Date == "2021-12-19") %>% ggplot(aes(y=Model, x=`Horizon (Weeks)`, fill=FreqDiscrete))+
  geom_tile(color= "white") + 
  scale_fill_viridis(name="Count of Predictions (Counties = 58)", direction=-1, discrete=TRUE) + 
  theme(axis.text.x = element_text(angle = 45, vjust = .5, hjust = .5)) +
  ggtitle("2021-12-19: Count of Submissions by Model and Horizon", subtitle = "Quantile = 0.100")
countovertime_Dec19_10
```

#### 2.5%

```{r echo=FALSE}
countovertime_Dec19_2.5 <- pointestimatecompleteness %>% filter(Quantile == 0.025, Date == "2021-12-19") %>% ggplot(aes(y=Model, x=`Horizon (Weeks)`, fill=FreqDiscrete))+
  geom_tile(color= "white") + 
  scale_fill_viridis(name="Count of Predictions (Counties = 58)", direction=-1, discrete=TRUE) + 
  theme(axis.text.x = element_text(angle = 45, vjust = .5, hjust = .5)) +
  ggtitle("2021-12-19: Count of Submissions by Model and Horizon", subtitle = "Quantile = 0.025")
countovertime_Dec19_2.5
```

## Per-Capita Estimate: California Population Data

As a control model, allocation score was run on a per-capita allocation. In this approach, allocation ignores all epidemiologic and related data, allocating purely based on each county's population. This allocation, we should note, stays constant throughout the period of interest.

California population data were sourced via the CA Department of Finance ([link here](https://dof.ca.gov/forecasting/demographics/estimates/)) - we used the 2020-2025 county level estimates.

## Resources to Allocate

For the two periods, resource constraints were set so that it would be difficult/impossible to avoid underallocation across the locations. Recall that in situations of ample resources, allocation scores can be 0, since loss is easier to avoid, and in situations of very scarce resources, the unavoidable loss is very high and can make up the bulk of the loss value, also leading to lower allocation scores. In contrast, as resources get closer to true need, allocation scores can be expected to rise - the scare resources mean that the decision-making is very important. Again, the raw allocation scores of the experiment do not matter (the comparison is between scores of different models evaluated in the same experiment), but experimental setups that establish a resource constraint closer to the true need will avoid scores of 0 and therefore make interpretation of the scores more meaningful.

```{r warning=FALSE, echo=FALSE}
ggplot(data=truth_data_ca, aes(x=target_end_date, y=value)) + 
  geom_point(color="grey75") + geom_line(color="grey75") + labs(x = "MMWR Week End Date", y ="Weekly Incident Cases", title = "California: Statewide Weekly Incident COVID-19 Cases", subtitle = "Horizonal lines show resource constraints")  + theme_bw() + theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust=1)) + geom_point(data = dates_of_interest, aes(x=target_end_date, y=value), color="#00BFC4") + geom_point(data = horizons_of_interest, aes(x=target_end_date, y=value), color="#C77CFF") +  
  geom_segment(aes(x = as.Date("2021-10-15"), y = 200000, xend = as.Date("2022-04-15"), yend = 200000), size=1, colour = "#7CAE00") + 
  geom_label(aes(label= "200,0000", y=200000, x=as.Date("2021-08-15")), size=3) +
  geom_segment(aes(x = as.Date("2020-09-15"), y = 100000, xend = as.Date("2021-04-15"), yend = 100000), size=1, colour = "#7CAE00") + 
  geom_label(aes(label= "100,0000", y=100000, x=as.Date("2020-07-15")), size=3)
```

For these reasons, we established resource constraints of 100,000 units and 200,000 units respectively for the

# Results

For each date, the selected models were run for weeks 1-4. Allocation scores were determined.

## November 21st, 2020 {.tabset}

```{r warning=FALSE, message=FALSE, include=FALSE, echo=FALSE}
# 1 week
tar_load("Kat100k_alloscores1")
tar_load("percap1_100")
tar_load("score_data1")

Kat100k_alloscores1 <- bind_rows(Kat100k_alloscores1, percap1_100 |> filter(`K` == 100000))

alloscore_data_summaries1 <- Kat100k_alloscores1 |>
  group_by(model, reference_date) |>
  mutate(components = case_when(              # This line gets rid of negatives by turning
    components <= 0 ~ abs(`components`),      # neg values (undershoots) to absolute value
    components > 0 ~  0                       # and pos values (overshoots) to 0. Comment 
  )) |>                                       # out this chunk to go back to the old method.
  summarize(alloscore = sum(components), yobs = sum(y)) |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         allo_rank = rank(alloscore, ties.method = "min"),
         allo_rank_std = 1- (allo_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 7)

alloscore_models <- unique(alloscore_data_summaries1$model)
score_data_summaries1 <- score_data1 |>
  mutate(reference_date = target_end_date - 7) |>
  filter(model %in% alloscore_models) |>
  group_by(model, reference_date) |>
  summarize(mwis = mean(wis)) |>
  ungroup() |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         mwis_rank = rank(mwis, ties.method = "min"),
         mwis_rank_std = 1- (mwis_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 7)

all_scores1_100 <- left_join(alloscore_data_summaries1, score_data_summaries1,
                      by=c("model", "target_end_date")) %>%
  filter(reference_date.x=="2020-11-21")

# 2 week
tar_load("Kat100k_alloscores2")
tar_load("percap2_100")
tar_load("score_data2")

Kat100k_alloscores2 <- bind_rows(Kat100k_alloscores2, percap2_100 |> filter(`K` == 100000))

alloscore_data_summaries2 <- Kat100k_alloscores2 |>
  group_by(model, reference_date) |>
  mutate(components = case_when(              # This line gets rid of negatives by turning
    components <= 0 ~ abs(`components`),      # neg values (undershoots) to absolute value
    components > 0 ~  0                       # and pos values (overshoots) to 0. Comment 
  )) |>                                       # out this chunk to go back to the old method.
  summarize(alloscore = sum(components), yobs = sum(y)) |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         allo_rank = rank(alloscore, ties.method = "min"),
         allo_rank_std = 1- (allo_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 14)

alloscore_models <- unique(alloscore_data_summaries2$model)
score_data_summaries2 <- score_data2 |>
  mutate(reference_date = target_end_date - 14) |>
  filter(model %in% alloscore_models) |>
  group_by(model, reference_date) |>
  summarize(mwis = mean(wis)) |>
  ungroup() |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         mwis_rank = rank(mwis, ties.method = "min"),
         mwis_rank_std = 1- (mwis_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 14)

all_scores2_100 <- left_join(alloscore_data_summaries2, score_data_summaries2,
                      by=c("model", "target_end_date"))%>%
  filter(reference_date.x=="2020-11-21")

# 3 week

tar_load("Kat100k_alloscores3")
tar_load("percap3_100")
tar_load("score_data3")

Kat100k_alloscores3 <- bind_rows(Kat100k_alloscores3, percap3_100 |> filter(`K` == 100000))

alloscore_data_summaries3 <- Kat100k_alloscores3 |>
  group_by(model, reference_date) |>
  mutate(components = case_when(              # This line gets rid of negatives by turning
    components <= 0 ~ abs(`components`),      # neg values (undershoots) to absolute value
    components > 0 ~  0                       # and pos values (overshoots) to 0. Comment 
  )) |>                                       # out this chunk to go back to the old method.
  summarize(alloscore = sum(components), yobs = sum(y)) |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         allo_rank = rank(alloscore, ties.method = "min"),
         allo_rank_std = 1- (allo_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 21)

alloscore_models <- unique(alloscore_data_summaries2$model)
score_data_summaries3 <- score_data3 |>
  mutate(reference_date = target_end_date - 21) |>
  filter(model %in% alloscore_models) |>
  group_by(model, reference_date) |>
  summarize(mwis = mean(wis)) |>
  ungroup() |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         mwis_rank = rank(mwis, ties.method = "min"),
         mwis_rank_std = 1- (mwis_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 21)

all_scores3_100 <- left_join(alloscore_data_summaries3, score_data_summaries3,
                      by=c("model", "target_end_date"))%>%
  filter(reference_date.x=="2020-11-21")

# 4 week

tar_load("Kat100k_alloscores4")
tar_load("percap4_100")
tar_load("score_data4")

Kat100k_alloscores4 <- bind_rows(Kat100k_alloscores4, percap3_100 |> filter(`K` == 100000))

alloscore_data_summaries4 <- Kat100k_alloscores4 |>
  group_by(model, reference_date) |>
  mutate(components = case_when(              # This line gets rid of negatives by turning
    components <= 0 ~ abs(`components`),      # neg values (undershoots) to absolute value
    components > 0 ~  0                       # and pos values (overshoots) to 0. Comment 
  )) |>                                       # out this chunk to go back to the old method.
  summarize(alloscore = sum(components), yobs = sum(y)) |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         allo_rank = rank(alloscore, ties.method = "min"),
         allo_rank_std = 1- (allo_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 28)

alloscore_models <- unique(alloscore_data_summaries2$model)
score_data_summaries4 <- score_data4 |>
  mutate(reference_date = target_end_date - 28) |>
  filter(model %in% alloscore_models) |>
  group_by(model, reference_date) |>
  summarize(mwis = mean(wis)) |>
  ungroup() |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         mwis_rank = rank(mwis, ties.method = "min"),
         mwis_rank_std = 1- (mwis_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 28)

all_scores4_100 <- left_join(alloscore_data_summaries4, score_data_summaries4,
                      by=c("model", "target_end_date"))%>%
  filter(reference_date.x=="2020-11-21")

```

### 1 week

```{r echo=FALSE}

summary1_100 <- all_scores1_100 %>% select(model, alloscore, allo_rank, mwis, mwis_rank) %>% drop_na(alloscore) %>% arrange(allo_rank)
kable(summary1_100, caption = "2020-11-21: Model scoring for the 1 week horizon (2020-11-28). Allocations conducted with a resource constraint of 100,000 units.")  
  
```

### 2 weeks

```{r echo=FALSE}
summary2_100 <- all_scores2_100 %>% select(model, alloscore, allo_rank, mwis, mwis_rank) %>% drop_na(alloscore) %>% arrange(allo_rank)
kable(summary2_100, caption = "2020-11-21: Model scoring for the 2 week horizon (2020-12-05). Allocations conducted with a resource constraint of 100,000 units.")
```

### 3 weeks

```{r echo=FALSE}
summary3_100 <- all_scores3_100 %>% select(model, alloscore, allo_rank, mwis, mwis_rank) %>% drop_na(alloscore) %>% arrange(allo_rank)
kable(summary3_100, caption = "2020-11-21: Model scoring for the 3 week horizon (2020-12-12). Allocations conducted with a resource constraint of 100,000 units.")
```

### 4 weeks

```{r echo=FALSE}
summary4_100 <- all_scores4_100 %>% select(model, alloscore, allo_rank, mwis, mwis_rank) %>% drop_na(alloscore) %>% arrange(allo_rank)
kable(summary4_100, caption = "2020-11-21: Model scoring for the 4 week horizon (2020-12-19). Allocations conducted with a resource constraint of 100,000 units.")
```

## December 18th, 2021 {.tabset}

```{r warning=FALSE, message=FALSE, include=FALSE, echo=FALSE}
# 1 week
tar_load("Kat400k_alloscores1")
tar_load("percap1_400")
tar_load("score_data1")

Kat400k_alloscores1 <- bind_rows(Kat400k_alloscores1, percap1_400 |> filter(`K` == 400000))

alloscore_data_summaries1 <- Kat400k_alloscores1 |>
  group_by(model, reference_date) |>
  mutate(components = case_when(              # This line gets rid of negatives by turning
    components <= 0 ~ abs(`components`),      # neg values (undershoots) to absolute value
    components > 0 ~  0                       # and pos values (overshoots) to 0. Comment 
  )) |>                                       # out this chunk to go back to the old method.
  summarize(alloscore = sum(components), yobs = sum(y)) |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         allo_rank = rank(alloscore, ties.method = "min"),
         allo_rank_std = 1- (allo_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 7)

alloscore_models <- unique(alloscore_data_summaries1$model)
score_data_summaries1 <- score_data1 |>
  mutate(reference_date = target_end_date - 7) |>
  filter(model %in% alloscore_models) |>
  group_by(model, reference_date) |>
  summarize(mwis = mean(wis)) |>
  ungroup() |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         mwis_rank = rank(mwis, ties.method = "min"),
         mwis_rank_std = 1- (mwis_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 7)

all_scores1_400 <- left_join(alloscore_data_summaries1, score_data_summaries1,
                      by=c("model", "target_end_date")) %>%
  filter(reference_date.x=="2021-12-25")

# 2 week
tar_load("Kat400k_alloscores2")
tar_load("percap2_400")
tar_load("score_data2")

Kat400k_alloscores2 <- bind_rows(Kat400k_alloscores2, percap2_400 |> filter(`K` == 400000))

alloscore_data_summaries2 <- Kat400k_alloscores2 |>
  group_by(model, reference_date) |>
  mutate(components = case_when(              # This line gets rid of negatives by turning
    components <= 0 ~ abs(`components`),      # neg values (undershoots) to absolute value
    components > 0 ~  0                       # and pos values (overshoots) to 0. Comment 
  )) |>                                       # out this chunk to go back to the old method.
  summarize(alloscore = sum(components), yobs = sum(y)) |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         allo_rank = rank(alloscore, ties.method = "min"),
         allo_rank_std = 1- (allo_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 14)

alloscore_models <- unique(alloscore_data_summaries2$model)
score_data_summaries2 <- score_data2 |>
  mutate(reference_date = target_end_date - 14) |>
  filter(model %in% alloscore_models) |>
  group_by(model, reference_date) |>
  summarize(mwis = mean(wis)) |>
  ungroup() |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         mwis_rank = rank(mwis, ties.method = "min"),
         mwis_rank_std = 1- (mwis_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 14)

all_scores2_400 <- left_join(alloscore_data_summaries2, score_data_summaries2,
                      by=c("model", "target_end_date"))%>%
  filter(reference_date.x=="2022-01-01")

# 3 week

tar_load("Kat400k_alloscores3")
tar_load("percap3_400")
tar_load("score_data3")

Kat400k_alloscores3 <- bind_rows(Kat400k_alloscores3, percap3_400 |> filter(`K` == 400000))

alloscore_data_summaries3 <- Kat400k_alloscores3 |>
  group_by(model, reference_date) |>
  mutate(components = case_when(              # This line gets rid of negatives by turning
    components <= 0 ~ abs(`components`),      # neg values (undershoots) to absolute value
    components > 0 ~  0                       # and pos values (overshoots) to 0. Comment 
  )) |>                                       # out this chunk to go back to the old method.
  summarize(alloscore = sum(components), yobs = sum(y)) |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         allo_rank = rank(alloscore, ties.method = "min"),
         allo_rank_std = 1- (allo_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 21)

alloscore_models <- unique(alloscore_data_summaries2$model)
score_data_summaries3 <- score_data3 |>
  mutate(reference_date = target_end_date - 21) |>
  filter(model %in% alloscore_models) |>
  group_by(model, reference_date) |>
  summarize(mwis = mean(wis)) |>
  ungroup() |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         mwis_rank = rank(mwis, ties.method = "min"),
         mwis_rank_std = 1- (mwis_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 21)

all_scores3_400 <- left_join(alloscore_data_summaries3, score_data_summaries3,
                      by=c("model", "target_end_date"))%>%
  filter(reference_date.x=="2022-01-08")

# 4 week

tar_load("Kat400k_alloscores4")
tar_load("percap4_400")
tar_load("score_data4")

Kat400k_alloscores4 <- bind_rows(Kat400k_alloscores4, percap3_400 |> filter(`K` == 400000))

alloscore_data_summaries4 <- Kat400k_alloscores4 |>
  group_by(model, reference_date) |>
  mutate(components = case_when(              # This line gets rid of negatives by turning
    components <= 0 ~ abs(`components`),      # neg values (undershoots) to absolute value
    components > 0 ~  0                       # and pos values (overshoots) to 0. Comment 
  )) |>                                       # out this chunk to go back to the old method.
  summarize(alloscore = sum(components), yobs = sum(y)) |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         allo_rank = rank(alloscore, ties.method = "min"),
         allo_rank_std = 1- (allo_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 28)

alloscore_models <- unique(alloscore_data_summaries2$model)
score_data_summaries4 <- score_data4 |>
  mutate(reference_date = target_end_date - 28) |>
  filter(model %in% alloscore_models) |>
  group_by(model, reference_date) |>
  summarize(mwis = mean(wis)) |>
  ungroup() |>
  group_by(reference_date) |>
  mutate(nmodels = n(),
         mwis_rank = rank(mwis, ties.method = "min"),
         mwis_rank_std = 1- (mwis_rank-1)/(nmodels-1)) |>
  ungroup() |>
  complete(model, reference_date) |>
  mutate(target_end_date = as.Date(reference_date) + 28)

all_scores4_400 <- left_join(alloscore_data_summaries4, score_data_summaries4,
                      by=c("model", "target_end_date"))%>%
  filter(reference_date.x=="2022-01-15")

```

### 1 week

```{r echo=FALSE}

summary1_400 <- all_scores1_400 %>% select(model, alloscore, allo_rank, mwis, mwis_rank) %>% drop_na(alloscore) %>% arrange(allo_rank)
kable(summary1_400, caption = "2021-12-18: Model scoring for the 1 week horizon (2021-12-25). Allocations conducted with a resource constraint of 400,000 units.")  
  
```

### 2 weeks

```{r echo=FALSE}

summary2_400 <- all_scores2_400 %>% select(model, alloscore, allo_rank, mwis, mwis_rank) %>% drop_na(alloscore) %>% arrange(allo_rank)
kable(summary2_400, caption = "2021-12-18: Model scoring for the 2 week horizon (2022-01-01). Allocations conducted with a resource constraint of 400,000 units.")  
  
```

### 3 weeks

```{r echo=FALSE}

summary3_400 <- all_scores3_400 %>% select(model, alloscore, allo_rank, mwis, mwis_rank) %>% drop_na(alloscore) %>% arrange(allo_rank)
kable(summary3_400, caption = "2021-12-18: Model scoring for the 3 week horizon (2022-01-08). Allocations conducted with a resource constraint of 400,000 units.")  
  
```

### 4 weeks

```{r echo=FALSE}

summary4_400 <- all_scores4_400 %>% select(model, alloscore, allo_rank, mwis, mwis_rank) %>% drop_na(alloscore) %>% arrange(allo_rank)
kable(summary4_400, caption = "2021-12-18: Model scoring for the 4 week horizon (2022-01-15). Allocations conducted with a resource constraint of 400,000 units.")  
  
```

# Insights
